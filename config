# Before use in Windows:
# - Disable 'python3.exe' in 'App execution aliases' in settings
# - Add 'C:\Program Files\Git\bin' to PATH to enable bash

Match exec "bash ~/.ssh/luna/install.sh -c ~/.ssh %h || echo Luna failed, trying the previous config >&2"
  Include ~/.ssh/config.inc
